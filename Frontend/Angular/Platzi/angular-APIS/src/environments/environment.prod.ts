export const environment = {
  production: true,
  API_URL: 'https://young-sands-07814.herokuapp.com', // Si estamos en produccion deberiamos ir a esta direccion y hacer la peticion de forma directa, el backend debe permitir los CORS hacia ese dominio, independiente de a donde hayamos hecho el deployment
  // Podemos hacerlo de forma directa sin necesidad de un proxy 
};
