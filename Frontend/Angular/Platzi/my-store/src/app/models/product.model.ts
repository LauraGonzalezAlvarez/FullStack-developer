export interface Product {// Interface producto que define la estructura del objeto  y el tipado 
    id: string;
    title: string;
    price: number;
    image: string;
    description: string;
    category: string;
  }